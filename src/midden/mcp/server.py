"""FastMCP server exposing the midden tool surface (spec.md §9).

Implemented in M4. The entry point exists now so the console script is not dangling.
"""

from __future__ import annotations


def main() -> None:
    """Run the MCP server over stdio."""
    raise SystemExit(
        "midden-mcp is not implemented yet — it lands in M4 (spec.md §11). "
        "The pipeline CLI is available now as `midden`."
    )
